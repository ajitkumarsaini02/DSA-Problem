class Solution {
public:
    double myPow(double x, int n) {
        if (n == 0) return 1;
        long long a=n;
        if (a < 0) {
            x=1/x;
            a=-a;
        }

        double y = myPow(x, a / 2);
        if (a % 2 == 0) return y * y;
        else return x * y * y;
    }
};